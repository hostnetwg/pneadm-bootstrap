<style>
    .campaign-form-section .card-header {
        border-bottom: 1px solid rgba(0, 0, 0, 0.06);
    }
    .campaign-form-section .card-body > :last-child {
        margin-bottom: 0 !important;
    }
    .campaign-field-hint-details summary {
        cursor: pointer;
        color: var(--bs-primary);
        user-select: none;
    }
    .campaign-field-hint-details summary:hover {
        text-decoration: underline;
    }
    .campaign-form-section-number {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 1.65rem;
        height: 1.65rem;
        border-radius: 50%;
        font-size: 0.8rem;
        font-weight: 600;
        flex-shrink: 0;
    }
    .campaign-form-actions {
        border-top: 1px solid var(--bs-border-color);
        background: var(--bs-light);
    }
</style>
